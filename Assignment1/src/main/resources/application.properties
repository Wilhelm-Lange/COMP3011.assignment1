spring.application.name=Assignment1

# The YAML wants timestamps as strings like "2026-07-14T01:15:30Z".
# Spring Boot 4 uses Jackson 3, so this setting lives under datatype.datetime
# (the older spring.jackson.serialization.* name does nothing here).
spring.jackson.datatype.datetime.write-dates-as-timestamps=false
